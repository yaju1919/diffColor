diffColor
(function() {
    'use strict';
    var h = $("<div>").appendTo($("body")).css({
        "text-align": "center",
        padding: "1em"
    });
    $("<h1>",{text:"色の距離の計算"}).appendTo(h);
    $("<div>",{text:"aaaa"}).appendTo(h);
})();
